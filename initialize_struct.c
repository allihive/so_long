#include "so_long.h"

void    init_struct(char **grid) //using the map 
{
    char *game;

    game->x = row_count(map);
    game->y = column_count(map);
    //put the grid of the game in
    //put the steps
    //count collectibles
    //player position
    //find the exit position

}
